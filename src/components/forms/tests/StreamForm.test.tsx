import { StreamForm } from "../StreamForm";

describe("<StreamForm />", () => {
  it("should render the component", () => {
    const component = "shallow(<StreamForm />)";
    expect(component).toEqual("shallow(<StreamForm />)");
  });
});
